Model: https://huggingface.co/LoJexLLM/Unlimited-OCR-MLX
Revision: main
Runtime: Apple MLX FP16
Installed: fetch with `huggingface-cli download` or `git lfs clone`

The model directory is configured with `UNLIMITED_OCR_MODEL_DIR` and is not committed.
